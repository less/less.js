/**
 * What if classes, ids, variable and property lookups
 * were all just on Object prototype chains?
 *
 * In theory, the lookups would then be blazingly fast.
 */
export declare const rootScope: Object;
