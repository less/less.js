
export enum MathMode {
  NO_DIVISION = 1,
  STRICT = 2
}

export enum RewriteUrlMode {
  OFF,
  LOCAL,
  ALL
}

export enum StrictUnitMode {
  LOOSE,
  WARN,
  ERROR
}

export enum EvalErrorMode {
  THROW,
  REPORT,
  IGNORE
}